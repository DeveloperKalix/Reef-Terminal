# Firebase service account key path (JSON file).
# Export this or set GOOGLE_APPLICATION_CREDENTIALS instead.
# FIREBASE_CREDENTIALS_PATH=./reef-media-trust-panel-firebase-adminsdk-fbsvc-39a64f5023.json

# Optional: use a profile from jac.toml (e.g. development)
# JAC_PROFILE=development
